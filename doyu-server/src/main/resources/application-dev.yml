spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/douyu
    username: douyu
    password: douyu_dev_password
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

douyu:
  storage:
    local-path: ${DOUYU_STORAGE_PATH:./doyu-storage}
    base-url: ${DOUYU_STORAGE_BASE_URL:http://10.64.241.153:8080}
